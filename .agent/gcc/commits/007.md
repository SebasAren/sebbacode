# Commit 007: Create planning_prompts.py with draft, critique, refine prompt templates
**Date**: 2026-03-25T17:20:06.229307

## What I Did
- Created src/sebba_code/planning_prompts.py with three main prompt functions plus a format validator
- Implemented draft_roadmap_prompt(state, context) - assembles context from L0/L1 memory, briefing, and git state with truncation to prevent token overflow
- Implemented critique_roadmap_prompt(state, draft) - validation checklist with format, content, and scope checks using severity levels (blocking/warning/suggestion)
- Implemented refine_roadmap_prompt(state, draft, critique) - applies critique fixes, maintains format, outputs refined roadmap
- Added format_validation_prompt() for regex-based format verification
- Added _truncate() helper with suffix to prevent context overflow
- Set max character limits for each context section (MAX_L0_CHARS=2000, MAX_RULES_CHARS=1500, etc.)
- Included todo format reference (_format_todo_checkbox) for consistent checkbox syntax
- Used AgentState type hints with TYPE_CHECKING to avoid circular imports

## Decisions Made
- Created new module instead of adding to prompts.py to keep planning-specific logic separate
- Used truncation helpers to prevent token overflow when feeding L0/L1/L2 memory into prompts
- Critique prompt instructs cheap model (Haiku) output format for easy parsing
- Followed seed.py roadmap format exactly (Goal, Context, Todos, Target Files, Active Branches, Decisions Made, Constraints)
- Todo format: - [ ] checkbox syntax to match read_roadmap regex expectations

## Issues Encountered
- Had to determine max character limits empirically - started with conservative limits that can be adjusted via constants if needed
- Used TYPE_CHECKING for AgentState to avoid circular import issues

## Files Touched
src/sebba_code/planning_prompts.py - new file with 4 prompt functions
