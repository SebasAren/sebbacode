# Commit 014: Added PlanningConfig with max_iterations, model, auto_approve options
**Date**: 2026-03-25T17:42:35.149547

## What I Did
- Added PlanningConfig dataclass to config.py with three fields: max_iterations (default 3), model (default ""), auto_approve (default False)
- Integrated PlanningConfig into AgentConfig as 'planning' field
- Updated load_config() to parse planning section from YAML
- Updated seed.py init_agent_structure() to include planning section in default config.yaml
- Updated planning.py _get_max_iterations() and _get_planning_model() to read from config file with CLI override precedence
- Updated cli.py plan() command to use config defaults when --iterations not provided, and pass planning_model to graph

## Decisions Made
CLI arguments take precedence over config file settings, which take precedence over hardcoded defaults. Empty model string falls back to default LLM. Used @dataclass decorator pattern consistent with existing config classes.

## Issues Encountered
None

## Files Touched
src/sebba_code/config.py (added PlanningConfig), src/sebba_code/seed.py (added planning YAML section), src/sebba_code/nodes/planning.py (updated _get_max_iterations/_get_planning_model), src/sebba_code/cli.py (updated plan() to use config)
