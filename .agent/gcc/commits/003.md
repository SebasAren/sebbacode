# Commit 003: Add --max-todos CLI option to override session todo limit
**Date**: 2026-03-25T14:08:16.054857

## What I Did
- Added DEFAULT_MAX_TODOS = 5 constant to constants.py
- Added max_todos: Optional[int] field to AgentState TypedDict in state.py
- Updated should_continue() in nodes/extract.py to read max_todos from state, falling back to DEFAULT_MAX_TODOS when None
- Added --max-todos argument to the run CLI command in cli.py, threading the value into initial_state

## Decisions Made
- Used Optional[int] in state to allow None as sentinel meaning "use default"
- Passed max_todos via initial_state dict rather than config to keep it in the graph state throughout execution
- Only set max_todos in initial_state when explicitly provided (not None) to avoid unnecessary state entries

## Files Touched
src/sebba_code/constants.py, src/sebba_code/state.py, src/sebba_code/nodes/extract.py, src/sebba_code/cli.py
