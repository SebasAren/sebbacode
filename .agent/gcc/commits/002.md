# Commit 002: Add status CLI command for agent state summary
**Date**: 2026-03-25T13:57:57.168810

## What I Did
- Added _count_todos() function to parse .agent/gcc/main.md and count [x] vs [ ] checkboxes using regex
- Added _count_gcc_commits() function to count total git commits via git rev-list --count HEAD
- Added _list_memory_files() function to recursively list files in .agent/memory/ directory
- Added status CLI command with formatted output showing roadmap progress, GCC commits, and memory files

## Decisions Made
- Used regex pattern ^\s*[-*]\s*\[([ x])\] to match both - and * list markers with optional whitespace
- Handled missing .agent/ directory gracefully with fallback messages
- Used subprocess.run with error handling for git commands to avoid crashes in non-git directories
- Listed memory files relative to .agent/memory/ for cleaner output

## Files Touched
src/sebba_code/cli.py (added status command and 3 helper functions)
