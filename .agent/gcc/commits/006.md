# Commit 006: Add `sebba-code plan` CLI command that passes user request into graph state
**Date**: 2026-03-25T17:14:46.672236

## What I Did
- Added `sebba-code plan "description"` CLI command to `src/sebba_code/cli.py`
- Imported `build_agent_graph` from `sebba_code.graph`
- Added `DEFAULT_PLANNING_MAX_ITERATIONS = 3` constant
- Implemented `plan()` function with:
  - Click command decorator with `description` argument and `--iterations`/`-i` option
  - LLM configuration via `_configure_llm_from_config()`
  - Agent directory initialization via `init_agent_structure()`
  - Graph construction via `build_agent_graph()`
  - Full initial state with all required fields (messages, roadmap, current_todo, target_files, briefing, exploration_mode, memory, working_branch, session_start_commit, todos_completed_this_session, max_todos)
  - Planning loop fields: user_request, draft_roadmap, planning_messages, planning_iteration, planning_complete
  - Recursion limit based on max_iterations
  - Streaming event handling with node name visibility
  - NotImplementedError handling with helpful error messages

## Decisions Made
- Used default constant for max_iterations since PlanningConfig doesn't exist yet in config.py (will be added when that todo is implemented)
- Used `init_agent_structure()` from seed.py to ensure directory structure exists (same pattern as `init` command)
- Handled NotImplementedError gracefully so users get helpful message when planning nodes aren't implemented yet
- Set recursion_limit to 100 + max_iterations * 10 to accommodate the planning loop overhead

## Issues Encountered
- `init_agent_dir()` takes a Path argument, but CLI group already sets it globally - used `init_agent_structure()` instead
- `config.planning.max_iterations` doesn't exist yet in config.py - used default constant instead

## Files Touched
src/sebba_code/cli.py - added plan command
