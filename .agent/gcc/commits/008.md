# Commit 008: Implemented `draft_roadmap` node - generates initial roadmap draft from user request + memory context via LLM
**Date**: 2026-03-25T17:33:06.016468

## What I Did
- Updated `src/sebba_code/nodes/planning.py` with full `draft_roadmap` implementation
- Added imports for `get_llm` from `llm.py` and `draft_roadmap_prompt` from `planning_prompts.py`
- Implemented `draft_roadmap(state, configurable)` that:
  - Extracts user_request, memory context, existing roadmap, briefing, and target_files from state
  - Builds additional context dict with file structure and existing roadmap (if any)
  - Calls `draft_roadmap_prompt(state, context)` to generate the prompt
  - Invokes main LLM via `get_llm()` (creative work, not cheap model)
  - Returns `{"draft_roadmap": content, "planning_iteration": 1}`
- Follows constraint: draft stays in state, no disk writes

## Decisions Made
- Used main LLM (via `get_llm()`) for draft generation since this is creative/implementation work, not simple validation
- Reused `draft_roadmap_prompt` from `planning_prompts.py` to keep prompts centralized
- Included existing roadmap in context dict for re-planning scenarios
- Set `planning_iteration` to 1 on initial draft generation

## Issues Encountered
- None - implementation was straightforward given existing prompt templates and state patterns

## Files Touched
src/sebba_code/nodes/planning.py - full implementation of draft_roadmap function
