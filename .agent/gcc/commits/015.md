# Commit 015: Added PlanningConfig dataclass with max_iterations, model, and auto_approve options to the config system, including CLI integration and YAML template support.
**Date**: 2026-03-25T17:42:49.562134

## What I Did
['Created PlanningConfig dataclass in src/sebba_code/config.py with three fields: max_iterations (default 3), model (default empty string), auto_approve (default False)', 'Integrated PlanningConfig into the existing AgentConfig structure', 'Added CLI arguments (--planning-max-iterations, --planning-model, --planning-auto-approve) to src/sebba_code/cli.py', 'Updated src/sebba_code/seed.py to include planning section in the YAML template', 'Modified src/sebba_code/nodes/planning.py to use the new config options', 'Verified configuration loading works correctly with defaults and CLI overrides', 'Ran tests to confirm no regressions', 'Committed changes as commit 014']

## Decisions Made
Used empty string for model default to fall back to system default; auto_approve uses bool type for CLI flag parsing; PlanningConfig nested within AgentConfig to keep planning options organized

## Files Touched
src/sebba_code/config.py, src/sebba_code/cli.py, src/sebba_code/seed.py, src/sebba_code/nodes/planning.py
