- [x] ~~Add module-level docstrings to all Python files under `src/sebba_code/` that are missing them. Each docstring should be a single concise sentence describing the module's purpose. Target files: `graph.py`, `state.py`, `config.py`, `llm.py`, `prompts.py`, `seed.py`, `cli.py`, `constants.py`, and all files in `nodes/`, `tools/`, and `helpers/` subdirectories.~~ → Added docstrings to all 24 target files (8 main modules, 8 nodes, 4 tools, 6 helpers). Excluded __init__.py and __main__.py as specified.
- [x] ~~Add a `--dry-run` flag to the `run` CLI command that prints the next todo from the roadmap and exits without executing it.~~ → Added --dry-run flag to run command with _get_next_todo() parser function. Correctly identifies uncompleted todos from .agent/gcc/main.md and exits before any graph execution.
- [x] ~~Add a `--debug-prompts` flag to the `run` CLI command. When enabled, log the full system/user prompt sent to the LLM at each node entry, and print a compact summary of each message in the state (role, token estimate or truncated content) after every LLM call in the execute loop.~~ → Added DEBUG_PROMPTS flag in constants.py, prompt logging in all 6 LLM call sites (execute, explore×3, context, extract), and per-message summaries in execute loop showing role, tool names, and content previews.
- [x] ~~Add a `status` CLI command that prints a summary of the current agent state: number of completed/pending todos from the roadmap, total GCC commits, and the list of memory files under `.agent/memory/`.~~ → Added three helper functions (_count_todos, _count_gcc_commits, _list_memory_files) and the status command to cli.py. Output shows roadmap progress, git commit count, and memory file listing.
- [x] ~~Add a `--max-todos` option to the `run` CLI command that overrides the default session limit of 5 todos. Pass the value through to the graph state so `should_continue` respects it.~~

## Constraints
- Only add docstrings — do not modify any existing code or logic.
- Skip `__init__.py` and `__main__.py` files.
- If a file already has a module docstring, leave it unchanged.
