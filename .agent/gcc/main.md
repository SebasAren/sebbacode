- [ ] Update `README.md` to reflect the current state of the project: describe the architecture (graph flow, tiered memory, execute subgraph), list all CLI commands (`run`, `init`, `seed`, `status`) with their flags (`--verbose`, `--dry-run`, `--debug-prompts`, `--max-todos`), and include setup/install instructions using `uv`.
