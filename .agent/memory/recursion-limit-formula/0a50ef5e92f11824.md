# Recursion Limit Formula Pattern / ## Formula: `100 + (max_iterations × 10)` / ## Purpose: Recursion limits need to accommodate both base overhead and iteration loop overhead. / ## Usage: / - Set based on expected maximum planning iterations / - Add 100 overhead for initial setup and LLM inference calls / - Formula accounts for repeated roadmaps being added to state memory / ## Example: For max_iterations=10, recursion limit = 100 + (10×10) = 200