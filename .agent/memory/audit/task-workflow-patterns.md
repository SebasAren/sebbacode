# Task Workflow Patterns — README Updates

For README updates: read source files → verify CLI commands from cli.py → extract tool metadata from __init__.py → verify config from config.py → write corrections → verify with run_command → commit with pattern 'docs: update [specific scope] per audit'. Objective commit messages with specific scope reference.
