When implementing Optional[T] sentinel patterns in TypedDict, the TODO title takes priority over description text. Example: todo titled 'draft_roadmap' with description referencing 'critique_roadmap' → implement as draft_roadmap node. This pattern applies across all node functions (node_function(state) -> state). LLM response mocking MUST return complete objects with all properties including message content and metadata. Pre-commit hook validation: maximum 1479 characters for shell script length. Mock patch target: when mocking functions imported via 'from X import Y', always patch at usage site (module_that_imports.Y) not at definition site. Config testing: manipulate environment variables via monkeypatch/os.environ for real-world validation scenarios.