EXEUTION TOOL TEMPLATE: When creating new execution tools, follow existing code.py pattern for architectural consistency. ALL execution tools MUST use subprocess for shell execution to maintain compatibility with existing node patterns. This ensures orthogonal concerns separate: execution tools handle raw shell operations, nodes handle state transformations. Test integration pattern: for any execute/integrate operations, always run module imports first to verify paths before pytest execution (order matters: verify import → run pytest). When testing Git-specific operations, use hybrid approach: mock subprocess calls for isolation testing + real Git execution for integration verification. Shell command error handling: implement cd-too-many-args fix for subdirectory navigation edge cases when testing from nested paths.