Git commit testing requires hybrid approach: combine mocked subprocess calls for isolation with real Git execution for integration verification. For LLM mock responses (especially invoke_with_timeout calls), return complete response objects with all properties to prevent partial match failures. Default model selection for L2→L1 summarization: use _get_cheap_model() prefixed helper + SEBBA_CHEAP_MODEL environment variable, leaving other model configs unchanged. Pre-commit hook enforces three validation targets: .gitignore patterns, README documentation consistency, codebase structure verification. Validation failure prevents commit until resolved. Path execution: always run pytest from correct repo root to avoid import failures (core principle verified in test-execution.md). Post-extraction hook triggers async summarization via summarize_to_l1 node with 50-character content-length gating for L2→L1 summarization activation.